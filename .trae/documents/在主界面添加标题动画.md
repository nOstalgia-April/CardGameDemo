我将在 `main.tscn` 主界面场景中添加标题动画。

**具体计划：**

1. **资源引入**：在 `main.tscn` 中引入 `res://assets/UI/Title/` 下的 `Title0.PNG` 到 `Title6.PNG` 共 7 张图片资源。
2. **创建节点**：

   * 在 `Main` 根节点下新建一个 `TextureRect` 节点，命名为 `TitleLogo`。

   * 将其位置设置为屏幕居中 (`anchors_preset = 8`)。
3. **配置动画**：

   * 在 `TitleLogo` 下新建一个 `AnimationPlayer` 节点。

   * 创建一个名为 `play_title` 的新动画。

   * **动画时长**：设置为 **0.2秒**（根据您的要求，这会是一个非常快速的动画，约 35帧/秒）。

   * **关键帧**：在 0.0s 到 0.2s 之间均匀插入关键帧，依次切换 `texture` 属性显示 `Title0` 到 `Title6`。

   * **自动播放**：设置该动画为自动播放（Autoplay）。

**待确认事项：**

* **动画速度**：0.2秒播放7帧意味着速度非常快（一眨眼的时间）。如果您是希望**每帧间隔 0.2秒**（总时长 1.4秒），请在确认时告知，否则我将严格按照 **总时长 0.2秒** 执行。

