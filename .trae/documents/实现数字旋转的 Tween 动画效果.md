根据您的需求，我将为您实现带有Tween动画的旋转效果，同时保证旋转后 NESW 数值逻辑正确。

### 核心思路
1. **视觉动画与逻辑分离**：
   - 逻辑上：继续使用 `rotate_numbers` 确保数值交换的准确性。
   - 视觉上：通过 Tween 动画让数字节点（DirNums/N, E, S, W）移动到目标位置，营造旋转的错觉。

2. **动画实现方案**：
   - 记录当前四个方向节点的位置。
   - 根据旋转方向（顺时针/逆时针），计算每个节点的目标位置（例如：顺时针时，N节点移动到E的位置）。
   - 使用 Tween 并行播放四个节点的移动动画。
   - **关键点**：动画结束后，瞬间重置节点位置，并刷新显示的数值。因为逻辑上的数值已经交换，所以视觉上看起来就像是数字真的转过去了。

### 具体修改计划
1. **修改 `unit_card.gd`**：
   - 重构 `rotate_numbers` 方法。
   - 引入 `play_rotation_animation(clockwise)` 函数。
   - 在动画播放期间锁定逻辑，或者让动画作为 `rotate_numbers` 的一部分执行（使用 `await`）。

**预期效果**：
当齿轮或 Sad 卡牌触发旋转时，玩家会看到四个数字平滑地飞向下一个位置，动画结束后，数值逻辑正确更新。

现在我将开始为您修改代码。