我已找到问题原因：关卡选择界面的单元格（`LevelSelectCell`）目前只实现了文本数字显示，而没有实现读取 `res://assets/Number/` 下的图片逻辑。

我们将按以下步骤修复：

1.  **修改 `scripts/level_select_cell.gd`**：
    *   **引入图片资源**：将 `Cell` 脚本中预加载数字图片（`ui_1.png` 到 `ui_9.png`）的代码复制过来。
    *   **动态创建图片节点**：在代码中动态创建一个 `TextureRect` 用于显示数字图片，使其居中显示。
    *   **更新显示逻辑**：在 `setup` 函数中，优先使用数字图片。如果数字在 1-9 范围内，隐藏原本的文本标签，显示对应的图片；如果超出范围（如第 10 关），则回退显示文本。

**具体代码变更点**：
*   在 `scripts/level_select_cell.gd` 中添加 `number_textures` 数组。
*   在 `_ready` 或初始化时创建 `level_number_rect`。
*   修改 `setup` 函数，根据 `index` 设置图片纹理。
